spring:
  application:
    name: user-server
  cloud:
    vault:
      uri: http://localhost:8200
    config:
      discovery:
        enabled: true
        service-id: config-server

eureka:
  client:
    service-url:
      defaultZone: ${DISCOVERY_URL}

spring:
  application:
    name: discovery-server

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:${SERVER_PORT}/eureka/

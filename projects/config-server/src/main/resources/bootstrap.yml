server:
  error:
    include-message: always
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: ${CONFIG_SERVER_URI}
          search-paths: '{application}'
        bootstrap: true

server:
  error:
    include-message: always
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_SERVER_URI}
          default-label: main
        bootstrap: true
